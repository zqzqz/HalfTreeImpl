[package]
name = "half_tree_dpf"
version = "0.1.0"
edition = "2021"

[dependencies]
aes = "0.8"
rand = "0.8"
rand_chacha = "0.3"
zeroize = { version = "1", features = ["derive"] }
subtle = "2"
bytemuck = { version = "1", features = ["derive"] }
thiserror = "1"

[dev-dependencies]
rand_chacha = "0.3"
